tmp*
tmp
profile
lcov.info
tarpaulin-report.html
/coverage/
.vscode
.idea
book
*.npy
repomix-output*

# Code Agent
# Shared agent instructions are tracked under `.claude.rstsr`.
# Symlink `.claude` (or `.agents`) to it for your agent; the symlink and any
# local-only agent state / caches / per-developer configs below are ignored.
.claude
.agents
.claude.json
.codex/

# Aider chat history & caches
.aider*

# Cursor / Continue / Roo local state (shared rules are committed separately)
.cursor/
.continue/
.roo/

# Catch-all for local-only files (e.g. *.local.toml, *.local.md)
*.local*

# CLAUDE.md or AGENTS.md at root directory
# Developer should customize their own file, and `.claude.rstsr/CLAUDE.md` serves as template
/CLAUDE.md
/AGENTS.md

# bindgen
blas_bindgen.h

# Generated by Cargo
# will have compiled files and executables
debug/
target/
build/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# Python scripts
.ipynb_checkpoints/
__pycache__/
*.pyc
